# Complete Command Index (A1-Z9)

All commands indexed alphabetically by category, numbered semantically within each category.

{% for category_name, category_data in categories.items() %}
**{{ category_data.display_name }}:** {{ category_data.commands | map(attribute='name') | join(', ') }}
{% endfor %}

**Note:** Command indexes are generated by code, not manually maintained. Use `meta/M8-identify-generative-content.md` to identify what should be automated.
